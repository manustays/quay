/**
 * Stub — replaced in Task 14.
 * Opens the add/edit form for a managed item.
 *
 * @param _item - Existing item to edit, or `null` to add a new one.
 * @param _onSave - Callback invoked after a successful save.
 */
export function openForm(_item: unknown, _onSave: () => void): void {}
