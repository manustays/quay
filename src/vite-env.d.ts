/// <reference types="vite/client" />

/** Human-readable app name, injected at build time from tauri.conf.json `productName`. */
declare const __APP_NAME__: string;
/** App version, injected at build time from package.json `version`. */
declare const __APP_VERSION__: string;
