# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# quay-hook helper staged for bundling — rebuilt by `npm run hook:build`
src-tauri/binaries/

# Updater manifest — generated at release time by scripts/make-latest-json.mjs and
# published as a GitHub Release asset; never committed.
latest.json

# Internal planning/strategy docs — kept locally, never committed (not for the
# public repo). Removed from tracking before open-sourcing.
docs/local-roadmap/
docs/superpowers/


# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
